spring:
  application:
    name: config-server
  profiles:
    active: git
  cloud:
    config:
      server:
        git:
          uri: https://github.com/RunSBS/BandFeed-config
          default-label: main
          search-paths: configs/{application}

server:
  port: 8888
